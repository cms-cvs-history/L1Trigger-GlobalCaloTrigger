

module gctemu = L1GctTest { 
    untracked bool doElectrons      = false
    untracked bool doEnergyAlgos    = false
    untracked bool doFirmware       = false
    untracked string inputFile      = ""
    untracked string referenceFile  = ""
    untracked string energySumsFile = ""
    uint32 preSamples = 2
    uint32 postSamples = 2
} 

# Setup the configuration for the GCT emulator
# The following line should work but has given problems in the muon trigger
#include "L1Trigger/Configuration/data/L1DummyConfig.cff"

# ... so instead, we choose just the .cff files relevant to the GCT
include "L1TriggerConfig/GctConfigProducers/data/L1GctConfig.cff"
include "L1TriggerConfig/L1ScalesProducers/data/L1CaloScalesConfig.cff"
include "L1TriggerConfig/L1GeometryProducers/data/l1CaloGeomConfig.cff"
